{
  "name": "new-medical-site",
  "version": "1.0.0",
  "description": "Sitio estático para New Medical Group con servidor de prueba para formulario",
  "main": "server/index.js",
  "scripts": {
    "preinstall": "node -e \"const {execSync}=require('child_process');try{execSync('cargo --version',{stdio:'ignore'})}catch(e){console.log('cargo no encontrado, skipping.');}\" || true",
    "start": "node server/index.js",
    "dev": "NODE_ENV=development node server/index.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5"
  }
}
